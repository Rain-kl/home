<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>net.arctel</groupId>
        <artifactId>fyrn</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>home-aggregation</artifactId>
    <name>Fyrn - Aggregation Module</name>
    <description>Fyrn 聚合启动模块, 整合所有服务模块作为统一入口</description>

    <dependencies>
        <!-- Fyrn 模块 -->
        <dependency>
            <groupId>net.arctel</groupId>
            <artifactId>fyrn-mms-service</artifactId>
        </dependency>
        <!-- Fyrn 模块 -->
        <dependency>
            <groupId>net.arctel</groupId>
            <artifactId>wb-service</artifactId>
        </dependency>

        <!-- framework 模块 -->
        <dependency>
            <groupId>net.arctel.platform</groupId>
            <artifactId>axion-data</artifactId>
        </dependency>
        <dependency>
            <groupId>net.arctel.platform</groupId>
            <artifactId>axion-web</artifactId>
        </dependency>

        <!-- OMS 模块 (通过 starter 自动装配 OMS 组件和 Mapper) -->
        <dependency>
            <groupId>net.arctel.platform</groupId>
            <artifactId>oms-starter</artifactId>
        </dependency>

      <!-- OPKIT 模块 -->
        <dependency>
            <groupId>net.arctel.platform</groupId>
            <artifactId>opkit-agent</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>net.arctel.platform</groupId>
            <artifactId>opkit-service</artifactId>
        </dependency>

        <dependency>
            <groupId>net.arctel.platform</groupId>
            <artifactId>opkit-app</artifactId>
        </dependency>

        <!-- Spring 相关 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- 数据库 -->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>druid-spring-boot-3-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>net.arctel.aggregation.AggregationApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>